import pygame
import os.path

# sprobowac z try i expect z importowaniem
#
# test
file_directory_list = []
for file in file_directory_list:
    if os.path.isfile(str(file)):
        pass
    else:
        print("no file " + str(file) + " found")


class Game:
    pass
